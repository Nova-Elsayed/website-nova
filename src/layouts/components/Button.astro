---
const { className, href, label } = Astro.props;
---

<a
  class={`block py-3 transition-all border rounded text-stone-600 border-stone-800 hover:scale-110 px-7 hover:border-stone-500 hover:text-stone-500 ${className ? className : ""}`}
  href={href}
  set:html={label}
/>
