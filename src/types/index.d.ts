export type Feature = {
  title: string;
  image: string;
};

export type Workshop = {
  title: string;
  image: string;
};

export type Button = {
  enable: boolean;
  label: string;
  link: string;
};
